package dashboard

import "local/taskmanager/internal/task"

templ CategoryTasks(tasks task.TaskList) {
  <div class="dashboard-fav-category-tasks fill-parent">
    <div>@CategoryHeader(tasks[0].Category, 0)</div>
    for _, task := range tasks {
      <div>@ListItem(task)</div>
    }
  </div>
}
