const RECURRING_INPUT_UNIT_SELECTOR = "#edit-task-recurring-unit"

const updateRecurringUnits = (event) => {
    let recurringValue = event.target.value;
  // get the select input 
  // loop through each child of the select input
  // if value == 1 
  //    if the last letter is 's', remove it (textContent)
  // else 
  //    if the last letter != 's', add it (textContent)
}
